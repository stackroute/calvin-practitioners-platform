const expect = require('chai').expect;
const memberInvitation = require('./communityinvitation.router');

// Set of test scenarios
describe('Added Member in the community', () => {
    // Test Cases for add New group in discourse tool
    it('New member added', (testDone) => {
        
    });

});
